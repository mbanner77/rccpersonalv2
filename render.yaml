services:
  - type: web
    name: rcpersonal-web
    runtime: node
    plan: starter
    region: frankfurt
    repo: https://github.com/mbanner77/rccpersonalv2.git
    buildCommand: ./scripts/render-build.sh
    startCommand: ./scripts/render-start.sh
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: DATABASE_URL
        fromDatabase:
          name: rcpersonal-db
          property: connectionString
      - key: CRON_SECRET
        generateValue: true
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        sync: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: SMTP_FROM
        sync: false
      - key: MANAGER_EMAILS
        value: "manager1@example.com,manager2@example.com"

  - type: cron
    name: rcpersonal-cron
    schedule: "0 8 * * *"
    runtime: node
    plan: starter
    region: frankfurt
    repo: https://github.com/mbanner77/rccpersonalv2.git
    buildCommand: npm ci
    startCommand: node scripts/cron-runner.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: CRON_SECRET
        generateValue: true
      - key: WEB_HOST
        fromService:
          name: rcpersonal-web
          type: web
          property: host

databases:
  - name: rcpersonal-db
    plan: starter
    region: frankfurt
