# Database
# PostgreSQL (Prod/Local via docker-compose)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/employee_anniversaries?schema=public"
# SQLite (Alt, nur Referenz)
# DATABASE_URL="file:./prisma/dev.db"

# SMTP for Nodemailer
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# Manager distribution list (comma-separated)
MANAGER_EMAILS="manager1@example.com,manager2@example.com"
